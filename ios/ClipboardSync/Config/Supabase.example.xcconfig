// Copy this file to Supabase.xcconfig in the same folder.
// The $() between the URL slashes prevents xcconfig from treating // as a comment.
SUPABASE_URL = https:/$()/your-project-ref.supabase.co
SUPABASE_ANON_KEY = your-publishable-or-anon-key
